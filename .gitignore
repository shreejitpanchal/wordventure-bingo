node_modules/
dist/
dev-dist/
scripts/logs/
coverage/

# Capacitor's generated Android/Gradle project -- fully regenerable via
# `npx cap add android`/`sync` from capacitor.config.ts + dist/
# (scripts/build_apk.ps1). Debug builds are auto-signed by Android's own
# throwaway debug keystore, so there's nothing here worth keeping.
android/
dist-apk/
