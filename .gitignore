node_modules/
dist/
dev-dist/
scripts/logs/
coverage/

# Bubblewrap's generated Android/Gradle project -- fully regenerable via
# `bubblewrap init`/`update` from the live PWA manifest (scripts/build_apk.ps1).
# Includes android/android.keystore, the APK signing key -- never commit that,
# back it up somewhere safe instead; losing it blocks future Play Store updates.
android/
dist-apk/
